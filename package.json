{
  "author": "Paul Serby <paul@serby.net>",
  "name": "configury",
  "description": "Easy management of environment based configuration",
  "version": "0.3.0",
  "tags": [],
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git@github.com:serby/configury"
  },
  "publishConfig": {
    "registry": "http://registry.npmjs.org"
  },
  "main": "configury.js",
  "scripts": {
    "lint": "jshint . --reporter=./node_modules/jshint-full-path/index.js",
    "pretest": "npm run-script lint",
    "test": "istanbul cover _mocha -- -R spec -r should",
    "posttest": "istanbul check-coverage",
    "prepublish": "npm test && npm prune"
  },
  "engines": {
    "node": ">=0.10"
  },
  "dependencies": {
    "lodash.merge": "^4.0.1",
    "traverse": "^0.6.6"
  },
  "devDependencies": {
    "istanbul": "^0.4.2",
    "jshint": "^2.9.1",
    "jshint-full-path": "^1.1.1",
    "mocha": "^6.1.4",
    "rewire": "^4.0.1",
    "should": "^13.2.3"
  }
}
